{
  "name": "pay.ubq.fi-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "deno run --allow-net --allow-read --allow-env --env-file server.ts",
    "start": "deno run --allow-net --allow-read --allow-env --env-file server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.2",
    "@supabase/supabase-js": "^2.57.4",
    "hono": "^4.9.6"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
