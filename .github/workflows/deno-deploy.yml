name: Deno Deploy

on:
  pull_request:
  push:
  workflow_dispatch:

jobs:
  deploy:
    permissions:
      contents: read
      issues: write
    uses: ubiquity/deno-deploy-workflow/.github/workflows/deno-deploy-reusable.yml@pr-comments-test
    with:
      project: pay-ubq-fi
      entrypoint: serve.ts
      bun_version: 1.3.4
      install_command: |
        bun install
      build_command: |
        bun run build
      build_env: |
        VITE_SUPABASE_URL=$SUPABASE_URL
        VITE_SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY
        SUPABASE_SERVICE_ROLE_KEY=$SUPABASE_SERVICE_ROLE_KEY
      project_secrets: |
        SUPABASE_URL=SUPABASE_URL
        SUPABASE_ANON_KEY=SUPABASE_ANON_KEY
        SUPABASE_SERVICE_ROLE_KEY=SUPABASE_SERVICE_ROLE_KEY
      runtime_env: |
        STATIC_DIR=dist
      include: |
        dist/**
      exclude: |
        .git
        .github
        node_modules
        coverage
        .turbo
        .next
        .vercel
      debug_fetch_fail: true
      required_env: |
        DENO_DEPLOY_TOKEN
        SUPABASE_URL
        SUPABASE_ANON_KEY
    secrets: inherit
